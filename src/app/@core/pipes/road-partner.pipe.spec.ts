import { RoadPartnerPipe } from './road-partner.pipe';

describe('RoadPartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new RoadPartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
