import { FileNameAndTypePipe } from './file-name-and-type.pipe';

describe('FileNameAndTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FileNameAndTypePipe();
    expect(pipe).toBeTruthy();
  });
});
