import { FilterMeetupCalendarPipe } from './filter-meetup-calendar.pipe';

describe('FilterMeetupCalendarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMeetupCalendarPipe();
    expect(pipe).toBeTruthy();
  });
});
